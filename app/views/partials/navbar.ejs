<nav class="text-white shadow-lg flex justify-center min-h-20 items-center">
        <div class="flex md:flex-row flex-col gap-2 items-center md:w-[1000px] md:justify-between">
            <div class=" flex text-2xl md:text-4xl text-center items-center gap-2">
                <div class="w-[64px] h-[64px] bg-cover bg-no-repeat" style="background-image: url(/images/pokeball-64.png);"></div>
                <div>Poke-Dex</div>
            </div>
                <ul class="flex text-2xl gap-4">
                    <% links.map((link) => { %>
                        <li>
                            <a class="hover:bg-grey-200 text-lg md:text-lg text-center px-4 py-2 rounded-lg" href=<%= link.link %>><%= link.name%></a>
                        </li>
                    <% }) %>
                </ul>
        </div>
</nav>